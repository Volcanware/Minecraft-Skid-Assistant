package skidmonke;

import javax.net.ssl.HttpsURLConnection;
import java.util.ArrayList;

public class yes {
    public static String code, currentln, decodedResponse, decryptedResponse, decryptedSeed, decryptedHWID;
    public static ArrayList response = new ArrayList();
    public static HttpsURLConnection connection;
}
